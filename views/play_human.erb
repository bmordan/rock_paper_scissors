  <hgroup>
    OK <%= @player.first[:name] %> are<br />you Ready?
  </hgroup>
  <ul>
  <% @hash.keys.each do |gesture| %>
    <li>
      <a href="/waiting?player=<%= @player.first[:name] %>&gesture=<%= gesture %>">
        <img src="/images/<%= gesture %>.svg" class="gesture-image">
      </a>
    </li>
  <% end %>
  </ul>